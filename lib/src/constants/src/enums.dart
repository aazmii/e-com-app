enum PaymentType {
  cash,
  card,
  digitalPayment;
}

enum AppLayout {
  horizontalView,
  verticalView,
}

enum DigitalPaymentType {
  digitalPayemnt,
  bkash,
  rocket,
  nagad,
  ukash,
  upay,
}

enum DiscountType { cash, percentage }
