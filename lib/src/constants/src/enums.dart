enum PaymentType {
  cash,
  card,
  onlinePayment,
  digitalPayment,
  none;
}

enum DigitalPaymentType {
  digitalPayemnt,
  bkash,
  rocket,
  nagad,
  ukash,
  upay,
}

enum AppLayout {
  horizontalView,
  verticalView,
}

enum DiscountType { cash, percentage }
